package ak.dev.khi_backend.khi_app.exceptions;

public enum ErrorCode {
    VALIDATION_ERROR,
    NOT_FOUND,
    BAD_REQUEST,
    CONFLICT,
    UNAUTHORIZED,
    FORBIDDEN,
    DB_ERROR,
    STORAGE_ERROR,
    EXTERNAL_ERROR,
    INTERNAL_ERROR
}
