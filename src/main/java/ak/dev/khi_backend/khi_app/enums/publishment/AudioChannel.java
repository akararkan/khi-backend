package ak.dev.khi_backend.khi_app.enums.publishment;

/**
 * AudioChannel — the channel layout of an audio file.
 *
 * Used in SoundTrackFile.audioChannel.
 */
public enum AudioChannel {

    /** Single channel (one speaker). */
    MONO,

    /** Two channels (left + right). */
    STEREO
}
